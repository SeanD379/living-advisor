# 模型化首页精简设计

**状态：已确认，待实现**

首页只保留正俯视的合租屋户型模型及其四个空间热点。移除模型下方的“待我处理”横滑卡片和“生活小事”快捷入口，不新建页面或数据结构。

费用、值日、物品和公约的详细操作继续通过既有底部导航与模型热点进入：费用在“费用”页，值日/物品/公约在“生活”页。模型仍展示任务状态点，并保留无障碍按钮文本和键盘焦点。

验收：手机首页的模型后直接接到底部导航；不再出现待办卡片或快捷入口；四个模型热点与现有费用、生活页面仍可访问。
